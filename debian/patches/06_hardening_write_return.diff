# Harden write and chdir because of ignored return value
Index: tmux/tty.c
===================================================================
--- tmux.orig/tty.c	2010-03-13 22:44:29.000000000 +0100
+++ tmux/tty.c	2010-03-13 23:28:05.038724665 +0100
@@ -300,7 +300,8 @@
 void
 tty_raw(struct tty *tty, const char *s)
 {
-	write(tty->fd, s, strlen(s));
+	if (write(tty->fd, s, strlen(s)) == -1)
+		fatal("write failed");
 }
 
 void
@@ -333,7 +334,8 @@
 	bufferevent_write(tty->event, s, strlen(s));
 
 	if (tty->log_fd != -1)
-		write(tty->log_fd, s, strlen(s));
+		if (write(tty->log_fd, s, strlen(s)) == -1)
+			fatal("write failed");
 }
 
 void
@@ -359,7 +361,8 @@
 	}
 
 	if (tty->log_fd != -1)
-		write(tty->log_fd, &ch, 1);
+		if (write(tty->log_fd, &ch, 1) == -1)
+			fatal("write failed");
 }
 
 void
Index: tmux/window.c
===================================================================
--- tmux.orig/window.c	2010-03-13 22:44:29.000000000 +0100
+++ tmux/window.c	2010-03-13 23:28:05.038724665 +0100
@@ -518,7 +518,9 @@
 		return (-1);
 	case 0:
 		if (chdir(wp->cwd) != 0)
-			chdir("/");
+			if (chdir("/") <0 )
+				fatal("chdir failed");
+
 
 		if (tcgetattr(STDIN_FILENO, &tio2) != 0)
 			fatal("tcgetattr failed");
